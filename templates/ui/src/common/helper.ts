
const docc = (tag: string) => document.createElement(tag)
const gid = (tag: string) => document.getElementById(tag)
const qss = (tag: string) => document.querySelector(tag)
const qas = (tag: string) => document.querySelectorAll(tag)


export {
    docc,
    gid,
    qss,
    qas,
}